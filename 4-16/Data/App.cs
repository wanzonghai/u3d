public class App
{
    // 使用 AssetBundleManager 类的静态成员
    public static AssetBundleManager ABMgr = AssetBundleManager.Instance;
    // 使用 EventManager 类的静态成员
    public static EventManager EventMgr = EventManager.Instance;
    // 使用 GameManager 类的静态成员
    public static GameManager GameMgr = GameManager.Instance;
    // 使用 PanelManager 类的静态成员
    public static PanelManager PanelMgr = PanelManager.Instance;
    // 使用 ResourceManager 类的静态成员
    public static ResourceManager ResMgr = ResourceManager.Instance;
    // 使用 SoundManager 类的静态成员
    public static SoundManager SoundMgr = SoundManager.Instance;

    // 使用 PanelObjectPool 类的静态成员
    public static PanelObjectPool panelPool = PanelObjectPool.Instance;

    // 使用 TipsManager 类的静态成员
    public static TipsManager tipsMgr = TipsManager.Instance;
}